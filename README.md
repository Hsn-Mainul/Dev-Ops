# Dev-Ops - git init , git add remote origin "" , git pull origin main , git status , git commit -m "", git add A , git log , git commit -a -m "all added" , git checkedout, git merge, rebase(another kind of merge), ssh-keygen.
# Dev-ops full course
